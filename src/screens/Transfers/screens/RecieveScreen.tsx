import React from 'react';
import {Text} from 'shared/styled-components/Texts';

type RecieveTransferScreenProps = {};

export const RecieveTransferScreen: React.FC<RecieveTransferScreenProps> = props => {
  return <Text> Recieve screen</Text>;
};
